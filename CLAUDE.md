@AGENTS.md

# Claude Code notes

Use `AGENTS.md` as the shared source of truth for repository workflow, validation, safety, and review rules.

For substantial or architectural changes, inspect the existing behavior and propose the implementation approach before editing. Keep the implementation scoped to the assigned GitHub issue and the current worktree/branch.

Do not create or use local Claude configuration as a substitute for committed project instructions; `.claude/` remains local-only.
