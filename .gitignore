# Project-specific Supabase credentials — copy config.sample.js to config.js and fill in your own.
# For Netlify, this file is instead generated at deploy time from environment variables (see DEPLOY.md).
config.js

# Build output — regenerated by build-deploy.ps1, not source
deploy/

# Design canvas payload — 2.5 MB of editor bundle, re-seeded from the .dc.html
# artboards + canvas.json beside it (see design/README.md), not source
design/overview-tab-review.html

# Large marketing collateral — not part of the app
MarketingCollateralGuide_v1b.pdf
TRIGO_CI_Guide_MINI_11_2020.pdf
Non-Rayong Manpower Management.png

# Local Claude Code tooling config, not app code
.claude/

# OS/editor cruft
.DS_Store
Thumbs.db
