/* Copy this file to config.js and fill in your Supabase project's values.
   Find these under Project Settings > API in your Supabase dashboard.
   The anon key is safe to expose in client-side code — Row Level Security
   (set up by supabase/schema.sql) is what actually protects the data. */
window.SUPABASE_CONFIG = {
  url: "https://YOUR-PROJECT-REF.supabase.co",
  anonKey: "YOUR-ANON-PUBLIC-KEY",
};
